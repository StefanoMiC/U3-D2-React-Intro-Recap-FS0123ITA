const ImageAndParagraph = props => (
  <>
    <img src={props.logo} className="App-logo" alt="logo" />
    <p>
      Edit <code>src/App.js</code> and save to reload.
    </p>
  </>
);

export default ImageAndParagraph;
